{
  "name": "serverless-get-segment-id",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "2.0.3",
    "axios": "^1.13.5",
    "twilio": "5.0.3",
    "twilio-flex-token-validator": "^1.6.0"
  },
  "devDependencies": {
    "twilio-run": "^5.0.0"
  },
  "engines": {
    "node": "^20.x || ^22.x"
  }
}
